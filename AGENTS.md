## Gallery Management
* The gallery is dynamically generated from `images/projects/` structure.
* After adding or modifying images in `images/projects/`, you MUST run `python tools/generate_gallery_data.py` to update `gallery_data.js`.
* Do not manually edit `gallery_data.js` or the HTML structure of `#gallery-container`.
